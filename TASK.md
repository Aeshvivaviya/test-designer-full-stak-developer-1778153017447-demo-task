# Sensussoft GymFlow: Member Check-in & Class Booking (Junior Full Stack)

> **Difficulty:** undefined | **Deadline:** 3 days

## Candidate
- **Name:** Test Designer
- **Role Applied:** full stak developer
- **Experience:** 1-2 years

---

## Scenario
Sensussoft serves boutique gyms that need a lightweight web portal for members to view class schedules, book sessions, and check in. As the assigned junior full stack developer, you will build a Node + React CRUD app that supports trainers and members with real-time availability and simple persistence.

---

## Requirements
1. Create a Node (Express) API with endpoints for classes (CRUD), bookings (create/cancel), and member check-ins; include validation and sensible HTTP status codes.
2. Build a React frontend with responsive UI listing classes, available slots, and member actions (book, cancel, check-in) using the API; implement client-side form validation and optimistic updates where appropriate.
3. Persist data using a JSON file or lightweight database (e.g., SQLite or lowdb) with seed data for classes, trainers, and members; ensure consistent reads/writes and basic error handling.
4. Implement search and filter on the frontend (by class type, date, trainer) and a simple availability indicator; ensure mobile-first, accessible markup and clear user feedback.

---

## Deliverables
- GitHub repository link with clear README (setup, run, test instructions and key decisions).
- Deployed preview (Vercel/Netlify + Render/Render-like) or local run instructions if deployment is not feasible.

---

## Evaluation Criteria
- ✅ Functional correctness: API contracts, CRUD operations, booking rules, and check-in behavior work as specified.
- ✅ Code quality: Readable, modular Node and React code with consistent naming, error handling, and safe data persistence.
- ✅ UX & responsiveness: Clear UI, helpful validation messages, search/filter, and smooth interactions across mobile and desktop.
- ✅ Delivery & reproducibility: Complete README, reproducible setup, sensible commit history, and optional deployed preview within the 3-day window.

---

## Submission
Submit your work as per the deliverables above and notify the Sensussoft hiring team.

*This task was AI-generated by Sensussoft Hiring System based on the candidate's profile.*
